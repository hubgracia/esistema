@model IEnumerable<isistema.Models.restauranteHora>

@{
    ViewBag.Title = "restaurantes";
}

<h2>restaurantes</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.restid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cardapioid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.restid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cardapioid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
